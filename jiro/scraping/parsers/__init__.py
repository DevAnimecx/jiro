"""Engine parsers. Importing this module registers engines in the registry.

Community parsers can register themselves by subclassing ``BaseEngine`` and
calling ``registry.register`` — no core changes required (PRD §6.1.6).
"""
