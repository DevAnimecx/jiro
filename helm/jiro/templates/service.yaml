apiVersion: v1
kind: Service
metadata:
  name: {{ include "jiro.fullname" . }}
  labels:
    {{- include "jiro.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jiro.selectorLabels" . | nindent 4 }}